
#include "my_str.h"
#include "MaList.h"

int	bubble_sort(t_ma_list *lst)
{
	t_ma_list *tmp = NULL;

	

	return 0;
}

int	order_list(t_main_data *strct, int opt)
{
  my_putstr(" -> in order list function\n");
	
	if (opt == 1)
		bubble_sort(strct->lst);

  return 0;
}
