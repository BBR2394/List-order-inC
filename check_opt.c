#include <stdlib.h>

#include "my_str.h"
#include "check_opt.h"


int check_opt(int ac, char **av, t_opt_list *optLst)
{
  t_opt_list	*lst;

  lst = malloc(sizeof(t_opt_list));
  my_putstr("dans check opt\n");
  
  return 0;
}
